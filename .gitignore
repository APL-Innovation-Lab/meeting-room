.DS_Store

# Environment — secret/real values live here and are never committed.
# The committed contract is .env.schema (and .env.example as a copyable template).
.env
.env.local
.env.*.local

# Varlock-generated env types (regenerated by the Vite plugin on dev/build).
env.d.ts

/node_modules/

# React Router
/.react-router/
/.cache
/build
/public/build
.output
/dist
